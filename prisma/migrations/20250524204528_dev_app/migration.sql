BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[Bubble_Hello] (
    [ID] INT NOT NULL,
    [Name] NVARCHAR(50),
    [Age] INT,
    CONSTRAINT [PK__Bubble_H__3214EC2748921763] PRIMARY KEY CLUSTERED ([ID])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
